{{#each items}}
  <div class="album_card">
    {{#if images.length}}
      <img src="{{this.images.0.url}}" alt="" />
    {{/if}}
    <div class="info">
    <h1>{{this.name}}</h1>
    <a href="/tracks/{{id}}">View Tracks</a>
    </div>
  </div>
{{/each}}